function calcDistance(X1, Y1, X2, Y2) {
    let planeX = X1;
    let planeY = Y1;
    let missileX = X2;
    let missileY = Y2;


    if ((missileX >= 180 && missileX <= 220) && (missileY >= 280 && missileY <= 320)) {
        return true;
    }

}